// Заглушка канала уведомлений (WhatsApp/Telegram) — реализация запланирована на
// Этап 2 (см. согласованный план в проекте). Когда будет подключён бот (Baileys
// для WhatsApp-группы, либо Telegram Bot API как запасной вариант), реальную
// отправку нужно вставить сюда — весь остальной код уже вызывает эту функцию
// в нужных местах (отметки прихода/ухода/визита) и менять больше ничего не придётся.
export async function notifyWhatsApp(message: string) {
  console.log("[Уведомление в общий чат]", message);
}
