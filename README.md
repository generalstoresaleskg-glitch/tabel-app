# Табель — график работы и учёт времени

Внутренняя система для 3 точек уходовой косметики: составление и утверждение
недельного графика, отметки прихода/ухода/визита, табель и выгрузка в CSV
(промежуточная замена автоматической выгрузки в Google Sheets).

Полный согласованный план (роли, процессы, роадмап) — в файле `PLAN.md` в этом
репозитории и в проекте на claude.ai.

## Что уже реализовано (Этап 1 + часть Этапа 2)

- Роли и вход по логину/паролю: владелец, управляющая, сотрудник (RBAC на
  уровне страниц и серверных экшенов). Управляющих может быть несколько;
  назначить роль "управляющая" может только владелец.
- Точки (филиалы): создание/редактирование/деактивация/удаление — только
  владелец.
- Сотрудники: создание и редактирование (управляющая может только рядовых
  сотрудников, роль меняет только владелец), смена пароля, деактивация.
- Владелец и управляющая **могут** быть назначены на график как подмена
  (обычная смена или визит) — наравне с рядовыми сотрудниками.
- Единая сетка графика (точки — строками, дни недели — столбцами, общая для
  всех ролей) с компактными аватарками участников вместо карточек с
  именем/временем — вся неделя помещается на экран телефона без
  горизонтальной прокрутки. Тап по ячейке открывает список назначенных,
  отметку прихода/ухода/визита и (для владельца/управляющей) назначение или
  снятие сотрудника. Время нигде в графике не показывается и не вводится —
  подставляется из часов работы точки и используется только в Табеле.
- Обязательное правило: на каждой активной точке в каждый день недели должна
  быть хотя бы одна обычная смена (SHIFT) — иначе отправить график на
  утверждение/публикацию нельзя (сервер и интерфейс блокируют одинаково).
- Цикл согласования: черновик → (владелец, только если черновик составляла
  управляющая) → публикация. Публикация не ждёт подтверждения от сотрудников —
  график сразу становится видимым всем. Текущая и следующая неделя
  редактируются независимо и показаны вместе на одной странице; более старые
  или дальние недели — по ссылке «Показать прошлые недели», в режиме только
  чтения.
- Правки уже опубликованного графика без повторного цикла согласования — с
  логом изменений (`schedule_change_log`).
- Отметки «Я на месте» / «Я ухожу» (с фиксацией опоздания/переработки) и
  «Посетила точку» для визит-задач.
- Табель: сводка по сотрудникам и детальный журнал по дням, с фильтром по
  месяцу. Экспорт в CSV (открывается в Google Sheets / Excel).
- PWA: приложение можно добавить на главный экран телефона (иконка, без
  адресной строки браузера). На телефоне — нижняя таб-панель (График/Табель);
  управление (Сотрудники/Точки) для владельца и управляющей — через профиль в
  шапке, с красной точкой-индикатором, если что-то требует их внимания
  (черновик ждёт утверждения или не хватает обязательного сотрудника).

## Что запланировано дальше (см. `PLAN.md`)

- Реальная отправка уведомления в общий чат WhatsApp об утверждении графика —
  отметки прихода/ухода/визита уже уведомляют через `src/lib/notify.ts`
  (сейчас заглушка, пишет в консоль сервера, готова к подключению
  Baileys-бота), уведомление об утверждении графика — сознательно отложено.
- Автоматическая ежедневная выгрузка в Google-таблицу (сейчас — CSV по
  кнопке).
- Расчёт ЗП — сознательно отложен пользователем на отдельный этап.
- Формальные заявки на отгул/больничный.

## Технологии

Next.js (App Router, Server Actions) + TypeScript + Tailwind CSS,
Drizzle ORM поверх Postgres (драйвер `@neondatabase/serverless`, оптимизирован
под serverless-хостинг вроде Vercel), NextAuth (Auth.js) v5 для логина по
паролю с ролями.

Задеплоенная версия использует Postgres через Neon (подключён к Vercel-проекту
как интеграция из Marketplace). Для локальной разработки подойдёт любой
Postgres — свой локальный сервер или тот же Neon (просто укажите его
`DATABASE_URL` в `.env.local`).

## Запуск локально

Понадобится доступ к любой Postgres-базе (локальной или облачной — например,
тот же бесплатный Neon).

```bash
npm install
cp .env.example .env.local
# впишите в .env.local свой AUTH_SECRET (команда для генерации — в самом файле)
# и DATABASE_URL — строку подключения к вашей Postgres-базе

npm run db:setup   # применяет миграции и заполняет базу тестовыми данными
npm run dev        # http://localhost:3000
```

### Тестовые учётные записи (после `npm run db:seed`)

| Роль | Логин | Пароль |
|---|---|---|
| Владелец | `owner` | `owner123` |
| Управляющая | `aigul` | `aigul123` |
| Сотрудник | `begimai` | `begimai123` |
| Сотрудник | `nurila` | `nurila123` |
| Сотрудник (СММ) | `aiperim` | `aiperim123` |

**Обязательно смените эти пароли перед реальным использованием** — сменить
можно на странице «Сотрудники» (кнопка «Сменить пароль»).

## Структура проекта

```
src/
  app/
    login/                 страница входа
    points/                точки (владелец)
    employees/             сотрудники (владелец/управляющая)
    schedule/               график: конструктор, согласование, отметки
    tabel/                  табель + CSV-экспорт
  components/
    week-grid.tsx             единая сетка графика (точки×дни, аватарки, окно по тапу)
    profile-menu.tsx           профиль в шапке: имя/роль, индикатор внимания, меню управления
    bottom-nav.tsx              нижняя таб-панель на телефоне (График/Табель)
  db/
    schema.ts               схема Drizzle (users, points, schedules, shifts, attendance, ...)
    seed.ts                  тестовые данные
  lib/
    session.ts               requireUser/requireRole
    dates.ts                  работа с неделями/месяцами
    notify.ts                 заглушка уведомлений (WhatsApp/Telegram)
    schedule-queries.ts        общие запросы к графику/табелю, обязательные слоты
    role-label.ts               подписи ролей (владелец/управляющая/сотрудник)
tests/
  smoke-core.mjs             старый сквозной сценарий (частично устарел после отказа от подтверждения сотрудниками)
  smoke-extended.mjs         менеджер/СММ/отметки прихода-ухода/CSV
  smoke-week-grid.mjs         новая сетка: профиль-меню, блокировка по обязательным сменам, назначение владельца/управляющей
  smoke-checkin-flow.mjs      публикация → отметка "я на месте" → архив недель только для чтения
PLAN.md                       согласованный план проекта
```

## Прогон smoke-тестов

Тесты используют Playwright и предустановленный Chromium.

```bash
npm run db:setup   # чистая база
npm run dev &      # сервер на 3000
node tests/smoke-week-grid.mjs
node tests/smoke-checkin-flow.mjs
```

## Деплой

Развёрнуто на Vercel + Postgres через Neon (подключена как интеграция из
Vercel Marketplace прямо к проекту — `DATABASE_URL` и сопутствующие
переменные добавлены в проект автоматически). Для WhatsApp-бота (Baileys)
понадобится отдельный постоянно работающий процесс (Railway/Fly.io —
serverless для этого не подходит, т.к. соединение с WhatsApp должно жить
постоянно) — это следующий шаг (Этап 2), пока не сделан.
